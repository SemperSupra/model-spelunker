name: experiment-0001-smoke

on:
  workflow_dispatch:
  push:
    branches:
      - 'research/experiment-0001-dcpmi'
    paths:
      - '.github/workflows/experiment-0001-smoke.yml'
      - 'tools/reference_cpu.py'
      - 'tools/run_dcpmi_reference.py'
      - 'fixtures/experiment-0001/**'
      - 'requirements-experiment-0001.txt'
  pull_request:
    paths:
      - '.github/workflows/experiment-0001-smoke.yml'
      - 'tools/reference_cpu.py'
      - 'tools/run_dcpmi_reference.py'
      - 'fixtures/experiment-0001/**'
      - 'requirements-experiment-0001.txt'

permissions:
  contents: read

jobs:
  reference-repeatability:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      HF_HOME: ${{ github.workspace }}/.cache/huggingface
      TOKENIZERS_PARALLELISM: 'false'
      TORCH_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      OPENBLAS_NUM_THREADS: '1'
      PIP_NO_CACHE_DIR: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pinned CPU PyTorch
        run: python -m pip install --index-url https://download.pytorch.org/whl/cpu 'torch==2.14.0+cpu'

      - name: Install pinned experiment dependencies
        run: python -m pip install -r requirements-experiment-0001.txt

      - name: Run DC-PMI reference repeatability gate
        run: |
          python tools/run_dcpmi_reference.py \
            --model EleutherAI/pythia-70m \
            --revision a39f36b100fe8a5377810d56c3f4789b9c53ac42 \
            --fixture fixtures/experiment-0001/reciprocity-dcpmi-heldout.jsonl \
            --repeats 2 \
            --output-dir artifacts/experiment-0001-dcpmi-reference

      - name: Summarize DC-PMI evidence
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          summary = json.loads(Path('artifacts/experiment-0001-dcpmi-reference/summary.json').read_text(encoding='utf-8'))
          primary = summary['primary']
          repeat = summary['intra_runner_repeatability']
          print('method=', summary['method'])
          print('fixture=', summary['fixture'])
          print('numeric_mode=', summary['numeric_mode'])
          print('cpu_model=', summary['runtime']['cpu_model'])
          print(f'reference_accuracy={primary["accuracy"]:.3f}')
          print(f'reference_cross_language_unanimous_rate={primary["cross_language_unanimous_rate"]:.3f}')
          print(f'intra_runner_selection_agreement={repeat["selection_agreement_rate"]:.3f}')
          print(f'intra_runner_max_margin_drift={repeat["max_margin_drift"]:.12g}')
          for language, stats in primary['by_language'].items():
              print(f'reference_{language}={json.dumps(stats, ensure_ascii=False, sort_keys=True)}')
          PY

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: experiment-0001-dcpmi-reference-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts
          if-no-files-found: error
          retention-days: 14
