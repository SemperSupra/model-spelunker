# Example repository

For contributor checks and repository validation, see `docs/VALIDATION.md`.

The helper in `scripts/quick_check.sh` is only a fast syntax check and is not the canonical substantive validation path.
